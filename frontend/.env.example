# API Base URL (for Vite build)
# Use /api/v1 when frontend is served behind same host as API (e.g. nginx proxy)
VITE_API_URL=/api/v1

# For local dev with proxy (vite.config.js proxies /api to backend)
# VITE_API_URL=/api/v1
